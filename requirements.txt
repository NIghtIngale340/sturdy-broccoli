# Exact versions verified working in Sprint 0 on Linux / Python 3.13 / CUDA 12.4.
# These are the versions that produced results/master_results.jsonl. Pin them.
#
# PyTorch must be installed with a CUDA wheel first, e.g.
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0

# Hugging Face stack
transformers==5.17.0
peft==0.21.0
accelerate==1.15.0
datasets==5.0.1
safetensors==0.8.0

# GGUF tensor-table inspection, used by src/quant_utils.py to measure real BPW.
# Installed from llama.cpp/gguf-py, which is pinned by the llama.cpp commit below.
gguf==0.19.0

# Scientific / analysis
numpy==2.5.3
scipy==1.18.1

# NOTE: bitsandbytes is deliberately NOT a dependency. The protocol forbids
# QLoRA; base models must be loaded in unquantized 16-bit precision.
#
# llama.cpp is not a pip dependency. Build it from the pinned commit recorded in
# docs/protocols/experiment_protocol.md section 1.2.
